namespace SocialActivistAPI.Common
{
    public enum Statuses 
    {
        Active = 1,
        NonActive = 2
    }

    public enum TransactionStates
    {
        Ordered = 1,
        Shipped = 2,
        Donated = 3
    }
}
