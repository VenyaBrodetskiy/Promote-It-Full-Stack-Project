# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET backend

on:
  workflow_dispatch:
#   push:
  pull_request:
    branches: [ "master" ]

jobs:
  build-and-test:

    runs-on: ubuntu-latest

    steps:
    - name: Get repo code
      uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x
    - name: Restore dependencies
      run: cd Backend/dotNetBackend; dotnet restore
    - name: Build
      run: cd Backend/dotNetBackend; dotnet build --no-restore
    - name: Test
      run: cd Backend/dotNetBackend; dotnet test --no-build --verbosity normal
